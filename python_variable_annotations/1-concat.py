#!/usr/bin/env python3
"""module that defines a function that concatenates"""


def concat(str1: str, str2: str) -> str:
    """function that takes two args(str) and concatenates them"""
    return str1 + str2
